spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gym?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false
    password: root
    username: root
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
  web:
    resources:
      static-locations: classpath:/templates/,classpath:/static/

mybatis:
  mapper-locations: classpath:mapper/*.xml

